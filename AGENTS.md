# Website

Read `../docs/WORKSPACE-AGENTS.md` for shared instructions and `README.md` for setup.

- Make changes easy for Ben to inspect in a local preview. Reuse an existing development server where possible.
- Check the build for code changes. Visually check affected layouts at desktop and mobile widths.
- Treat a push to `main` as a possible production deployment; a local commit is a separate action.
- Read current routes and data loaders before using old design notes. Keep archived designs as history.
